BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[CompletedSection] ADD [correctCount] NVARCHAR(1000),
[timeTaken] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
